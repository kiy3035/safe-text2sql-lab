# 저장소 작업 규칙

## 1. 프로젝트 목적

이 저장소는 `자연어 → LLM SQL 생성 → SQL 검증 → 읽기 전용 실행` 흐름을 직접 구현하고, 그 안전성과 정확도를 재현 가능한 실험으로 검증하는 개인 기술 블로그 프로젝트다.

- 회사 프로젝트나 상용 운영 경험처럼 표현하지 않는다.
- 스키마와 데이터는 모두 합성 데이터로 만든다.
- 블로그의 수치와 결론은 실제 실행 결과에서만 가져온다.
- 목표는 “LLM이 안전하다”를 주장하는 것이 아니라, 신뢰할 수 없는 LLM 출력을 실행 전에 어떻게 제한했는지와 남는 한계를 보여주는 것이다.

구현을 시작하기 전에 루트의 `PROJECT_SPEC.md`를 끝까지 읽고 따른다.

## 1.1 로컬 실행 환경 제약

이 프로젝트는 AWS나 유료 클라우드에 배포하지 않는다. 모든 구현과 실험은 로컬 환경에서 무료로 재현할 수 있게 구성한다.

- PostgreSQL은 Docker Compose로 실행한다.
- 자동 테스트는 Scripted/Fake LLM과 WireMock을 사용한다.
- 자연어 50건 실제 정확도 측정은 Ollama 로컬 LLM으로 수행한다.
- OpenAI API를 포함한 모든 종량제·유료 외부 API와 유료 SaaS를 사용하지 않는다.
- 무료 크레딧, 무료 체험, 결제수단 등록이 필요한 서비스도 사용하지 않는다.
- 유료 서비스로 자동 전환되는 fallback이나 선택적 adapter도 구현하지 않는다.
- 외부 결제 없이 프로젝트의 구현, 테스트, 실험, 문서화를 모두 완료할 수 있어야 한다.
- Ollama 연동은 `SqlGenerator` 구현체로 분리한다.
- 로컬 LLM 모델명과 버전은 설정값으로 주입한다.
- AWS, RDS, EC2, 유료 모니터링 서비스는 사용하지 않는다.
- 어떤 요구사항이 무료 로컬 도구만으로 수행 불가능하면 유료 서비스를 임의로 사용하지 말고 작업을 멈춘 뒤 사용자에게 보고한다.
- 실행 결과에는 CPU, RAM, OS, PostgreSQL 버전, Ollama 및 로컬 LLM 모델 정보를 기록한다.
- `PROJECT_SPEC.md` 또는 다른 문서의 외부 유료 LLM API 전제와 충돌하면 이 절을 우선하고, 해당 문서를 Ollama 기준으로 수정한다.

## 2. 저장소를 다루는 방식

- 기존 코드가 있으면 빌드 도구, 패키지 구조, 코딩 스타일을 먼저 파악하고 존중한다.
- 빈 저장소라면 Java 21, Spring Boot 3.x, Gradle, PostgreSQL을 기본으로 구성한다.
- 사소한 선택은 합리적인 기본값으로 진행하고, 결정 근거를 `docs/decisions.md`에 짧게 남긴다.
- 사용자가 만든 변경이나 무관한 파일을 덮어쓰거나 되돌리지 않는다.
- 데이터베이스의 인덱스 생성·삭제 같은 실험성 변경은 로컬 Docker 실험 DB에서만 수행한다.
- 비밀값을 코드, 예제 설정, 로그, 테스트 결과에 넣지 않는다. `.env.example`에는 키 이름만 둔다.

## 2.1 장기 작업 분할과 체크포인트

이 프로젝트는 작업량이 많으므로 한 번의 대화에서 전체 구현을 무리하게 완료하려고 하지 않는다.

- 작업 시작 전에 전체 범위를 독립적으로 검증 가능한 단계로 나눈다.
- 기본적으로 한 번의 대화에서는 하나의 주요 단계만 완료한다.
- 작업량이 작고 다음 단계까지 안전하게 검증할 수 있을 때만 인접한 단계를 함께 진행한다.
- 오래 걸리거나 사용량을 많이 소모할 것으로 예상되면 새 단계를 시작하지 않고 현재 단계를 검증 가능한 상태로 마무리한다.
- 중간에 멈추더라도 컴파일되지 않는 코드, 절반만 변경된 설정, 실패 원인을 알 수 없는 상태를 남기지 않는다.
- 각 단계가 끝나면 해당 단계의 테스트를 실행하고 실제 결과를 확인한다.
- 완료하지 않은 작업을 완료했다고 표현하지 않는다.
- 측정하지 않은 수치나 결과를 추정해 기록하지 않는다.

작업 진행 상황은 루트의 `PROGRESS.md`에 계속 기록한다. 파일이 없으면 생성한다.

`PROGRESS.md`에는 다음 내용을 유지한다.

1. 완료한 작업
2. 실제 실행한 테스트와 결과
3. 현재 정상 동작하는 기능
4. 미완료 작업
5. 발생한 오류와 확인된 원인
6. 다음 대화에서 바로 시작할 작업
7. 실행 및 재현 명령어
8. 변경한 주요 파일

각 단계가 끝나면 사용자에게 다음 내용을 보고하고 작업을 멈춘다.

- 이번 단계에서 완료한 내용
- 실행한 테스트와 실제 결과
- 현재 프로젝트 상태
- 다음 단계에서 진행할 내용

사용자가 `계속 진행해`라고 요청한 뒤에만 다음 주요 단계를 시작한다. 다음 작업을 시작할 때는 `AGENTS.md`, `PROJECT_SPEC.md`, `PROGRESS.md`를 먼저 읽고 이미 완료된 작업을 다시 구현하지 않는다.

권장 작업 단계는 다음과 같다.

### 1단계: 프로젝트와 로컬 DB 환경

- Spring Boot 프로젝트 구성
- PostgreSQL Docker Compose 구성
- Flyway 스키마와 합성 데이터 구성
- migration 계정과 read-only 계정 분리
- 애플리케이션 및 DB 실행 확인

### 2단계: LLM 연동

- `SqlGenerator` 인터페이스
- Ollama 구현체
- Scripted/Fake 구현체
- 프롬프트와 환경설정
- WireMock 기반 연동 테스트

### 3단계: SQL 검증 게이트

- SQL AST parser 연동
- SELECT-only 검증
- Table/Column/Function Allowlist
- alias와 서브쿼리 검증
- 악의적 SQL 20종 차단 테스트

### 4단계: SQL 실행과 재시도

- `ValidatedSelect` 기반 Native Query 실행
- 결과 행 제한과 statement timeout
- 최대 2회 재생성
- 지수 백오프
- API 및 통합 테스트

### 5단계: 측정 실험

- 자연어 질의 50건
- Golden SQL 결과 비교
- 인덱스 전후 실행계획 비교
- LLM 500 오류 재시도 측정
- CSV와 JSON 결과 생성

### 6단계: 문서와 블로그

- 아키텍처 문서
- 위협 모델과 남아 있는 한계
- 실험 결과 보고서
- 실제 측정값 기반 블로그 초안
- README 재현 절차 검증

## 3. 권장 구조와 의존성 원칙

기존 저장소가 비어 있을 때의 기본 선택은 아래와 같다.

- Java 21 / Spring Boot 3.x / Gradle
- PostgreSQL + Flyway
- JPA `EntityManager.createNativeQuery(...)` 기반 실행
- SQL AST 파서(JSqlParser 등) 기반 검증
- Testcontainers(PostgreSQL), JUnit 5
- WireMock 또는 MockWebServer를 이용한 LLM HTTP 오류 재현
- LLM 연동은 `SqlGenerator` 인터페이스 뒤에 둔다.
  - 실제 측정용 Ollama HTTP 구현체
  - 단위·통합 테스트용 Scripted/Fake 구현체

새 라이브러리를 추가할 때는 꼭 필요한 이유가 있어야 한다. SQL 안전성의 핵심을 정규식, 문자열 포함 검사, LLM 자기검증에 맡기지 않는다.

## 4. 보안 불변 조건

다음 규칙은 편의를 위해 약화하지 않는다.

1. LLM 출력은 항상 신뢰할 수 없는 문자열로 취급한다.
2. 파싱이나 정책 검증에 실패한 SQL은 실행 경로에 도달할 수 없어야 한다.
3. 한 번에 정확히 한 개의 조회문만 허용한다.
4. DDL/DML/DCL, 다중 문장, CTE, `UNION/INTERSECT/EXCEPT`, 잠금 구문, 시스템 카탈로그 접근을 거부한다.
5. 모든 테이블과 컬럼 참조를 AST 전체에서 재귀적으로 수집하여 Allowlist와 비교한다. SELECT 절뿐 아니라 JOIN, WHERE, GROUP BY, HAVING, ORDER BY, 서브쿼리도 검사한다.
6. `SELECT *`, `table.*`는 거부한다. 함수도 별도 Allowlist를 통과해야 한다.
7. 스키마 한정 이름은 허용된 스키마만 인정하며, 대소문자·인용 식별자·별칭으로 검증을 우회할 수 없어야 한다.
8. 응답 행 수 상한과 DB statement timeout을 강제한다.
9. 애플리케이션의 `readOnly=true`만 믿지 않는다. 쓰기 권한이 없는 별도 DB 계정과 최소 컬럼 권한을 함께 적용한다.
10. 검증 성공 SQL만 Native Query로 실행한다. 원문 SQL을 다른 문자열과 이어 붙이지 않는다.
11. 재시도 중에도 매 시도마다 새 SQL을 동일한 검증 게이트에 통과시킨다.
12. 검증기 오류, 파서 미지원 구문, 식별자 해석 불가 상황은 모두 fail-closed로 처리한다.

정규식이나 키워드 검사는 빠른 사전 거절용으로만 사용할 수 있다. 최종 허용 판단은 AST와 Allowlist가 담당한다.

## 5. 재시도 규칙

- 최초 요청을 포함한 전체 LLM 호출 예산은 최대 3회다. 즉, 재시도/재생성은 최대 2회다.
- 기본 백오프는 첫 재시도 200ms, 두 번째 재시도 400ms이며 설정 가능하게 만든다.
- 테스트에서는 실제 sleep 대신 주입 가능한 `Sleeper` 또는 `BackoffPolicy`로 지연값과 호출 횟수를 검증한다.
- LLM HTTP 5xx, SQL 파싱/정책 검증 실패, 재생성으로 회복 가능한 SQL 식별자 오류만 예산 안에서 재시도한다.
- LLM 4xx, 인증 실패, DB 권한 오류, statement timeout, 취소 요청은 재시도하지 않는다.
- 실패한 SQL은 어느 시도에서도 실행하면 안 된다.

## 6. 테스트와 측정 원칙

- 보안 테스트는 “예외가 났다”가 아니라 `executor 호출 횟수 = 0`까지 확인한다.
- 악의적 SQL 20종은 고정 fixture로 버전 관리한다.
- 정상 자연어 50건의 정확도는 SQL 문자열 일치가 아니라 고정 seed DB에서 Golden SQL과 생성 SQL의 결과 집합을 비교해 계산한다.
- 순서가 의미 없는 질문은 정규화한 unordered 비교, 순위 질문은 ordered 비교, 단일 값은 scalar 비교를 사용한다.
- 인덱스 실험은 `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` 결과를 원본 JSON과 요약 CSV로 함께 보존한다.
- 실행 환경, 데이터 건수, 모델명, temperature, 프롬프트 hash, commit SHA, 실행 시각을 결과에 기록한다.
- 평균만 제시하지 말고 반복 횟수, 중앙값, 최소/최대 또는 p95를 함께 남긴다.
- Ollama가 실행되지 않거나 로컬 모델이 준비되지 않았으면 자연어 50건을 측정한 척하지 않는다. 자동 테스트까지만 완료하고 실측 결과는 `PENDING`으로 두며, 모델 설치 및 실행 명령을 남긴다.
- 특정 테스트가 실패하면 결과를 지우지 말고 실패 원인과 재현 명령을 남긴다.

## 7. 완료 전에 반드시 확인할 것

- 전체 테스트 실행
- 보안 fixture 20건 모두에서 Native Query executor 미호출 확인
- LLM 500→500→200 시나리오가 정확히 3회 호출 후 성공하는지 확인
- 500→500→500은 3회 후 종료, 400은 즉시 종료하는지 확인
- 허용된 단순 조회, JOIN, 집계, 서브쿼리가 정상 실행되는지 확인
- 금지 테이블·금지 컬럼·함수·UNION·다중 문장이 차단되는지 확인
- README의 명령을 빈 환경 기준으로 다시 실행할 수 있는지 확인
- `docs/blog-draft.md`의 모든 수치가 결과 파일과 일치하는지 확인

## 8. 최종 보고 형식

작업을 마치면 다음 순서로 짧고 구체적으로 보고한다.

1. 구현한 전체 흐름
2. 보안상 핵심 결정과 남는 한계
3. 실행한 테스트와 실제 결과
4. 실험 결과 파일 및 블로그 초안 위치
5. 실행하지 못한 항목과 정확한 이유

측정하지 않은 값을 추정하거나 성공한 것처럼 표현하지 않는다.
